// JavaScript source code

let tableau = [4, 3, 5, 8];
tableau.sort();


tableau.forEach(function (element) {
    console.log(element);
});

//2
for (let element in tableau) {
    console.log(element);
}