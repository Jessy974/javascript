// JavaScript source code
let uno = "01";
let dos = "02";

let resultat = Number(uno) + Number(dos);
console.log(resultat);
//2
uno = Number(uno);
dos = number(dos);
if (!Number.isNaN(uno) && !Number.isNaN(dos)) {
    console.log(uno + dos);
} else {
    console.log('erreur de type');
}